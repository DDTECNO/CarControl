@model IEnumerable<CarControl.Domain.ViewModel.VeiculoViewModel>

@{
    ViewData["Title"] = "Veículos cadastrados";
}

<h1 class="Titulo">Veículos cadastrados</h1>

<p>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdVeiculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlacaVeiculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NmCondutor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CpfCondutor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdVeiculo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlacaVeiculo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NmCondutor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CpfCondutor)
            </td>
            <td>

                @Html.ActionLink("Editar", "EditarVeiculo", new {  id=item.IdVeiculo  }) |
                @Html.ActionLink("Detalhes", "DetalhesDoVeiculo", new {  id=item.IdVeiculo}) |
                @Html.ActionLink("Excluir", "Excluir", new { id=item.IdVeiculo }) |
                <a href="@Url.Action("RegistroDeEntrada", "Movimento")?IdVeiculo=@item.IdVeiculo">Registrar entrada</a> |
                <a href="@Url.Action("RegistroDeSaida", "Movimento")?IdVeiculo=@item.IdVeiculo">Registrar saída</a>
            </td>
        </tr>
}
    </tbody>
</table>
<a asp-controller="Home" asp-action="Index">Voltar ao menu  |</a>
<a asp-controller="Veiculo" asp-action="CadastroDeVeiculo">Inserir novo Veículo  |</a>
<a asp-controller="Movimento" asp-action="RegistroDeEntrada">Registrar nova entrada |</a>
<a asp-controller="Movimento" asp-action="RegistroDeSaida">Registrar saída |</a>
<a asp-controller="Vagas" asp-action="ConsultarVagas">Consultar vagas  |</a>

